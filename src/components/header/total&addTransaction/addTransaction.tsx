import Stack from '@mui/material/Stack';
import Button from '@mui/material/Button';

export default function AddTransactionButton() {
    return (
        <Button style={{background:'green'}} variant="contained">add transaction</Button>
    );
}
